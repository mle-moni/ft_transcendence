<div id="checkRoomPresence"></div>
<div id="userID" value="{{currentUser.id}}"></div>

<div class="col-md-8" 
  id="room_messages" 
  data-channel-subscribe="room" 
  data-room-id="{{currentRoom.id}}">
    ...
</div>

<br>
<br>

<a class="btn btn-primary" href="#room" role="button"> Back </a>

<br>

<div class="container">
    <div class="row">
        <div class="col">

            <h1>{{titleText}}</h1>
            <form id="{{formID}}" action="/api/room_messages.json" accept-charset="UTF-8" method="{{method}}">
                <input type="hidden" name="authenticity_token" value="{{token}}">

                {{!-- Room Data --}}
                <label for="roomName">Message </label>
                <input type="text" id="message" name="room_message[message]">

                <input type="hidden" id="roomOwner" name="room_message[user_id]" value="{{currentUser.id}}">
                <input type="hidden" id="roomID" name="room_message[room_id]" value="{{currentRoom.id}}">


                <br><br>
                <input type="submit" value="{{submitText}}">
                <br>
            </form>

        </div>
    </div>

    <br><br>
    <div class="container">
        <div class="row">
            <div class="col">

                <ul class="list-group">

                    {{!-- <div class="chat" data-channel-subscribe="room" data-room-id="{{currentRoom.id}}>"> --}}
                        {{#each roomMessages}}

                        {{#when user_id "noteq" ../currentUser.id}}
                        <li style="background-color: grey;" class="list-group-item">

                            <div class"otherMessage">
                                <img src="{{#getUserAttributes ../members user_id "image"}}{{/getUserAttributes}}"
                                    alt="Avatar" class="avatar">
                                {{#getUserAttributes ../members user_id "nickname"}}{{/getUserAttributes}} :
                                <p style="display: inline;" data-role="message-text">
                                    {{message}}
                                </p>
                            </div>

                        </li>
                        <br>
                        {{/when}}

                        {{#when user_id "eq" ../currentUser.id}}
                        <li style="background-color:lightskyblue" class="list-group-item">

                            {{!-- <div class"ownMessage"> --}}
                            <div class"otherMessage">

                                <img src="{{#getUserAttributes ../members user_id "image"}}{{/getUserAttributes}}"
                                    alt="Avatar" class="avatar">
                                {{#getUserAttributes ../members user_id "nickname"}}{{/getUserAttributes}} :
                                <p style="display: inline;" data-role="message-text">
                                    {{message}}
                                </p>
                            </div>

                        </li>
                        <br>
                        {{/when}}

                        {{/each}}
                    {{!-- </div> --}}

                </ul>


        </div>
    </div>





    {{!-- <div class="d-none" data-role="message-template">
        <li style="background-color: grey;" class="list-group-item">

            <div class"otherMessage">
                <p style="display: inline;" data-role="message-text">

                </p>
                <img src="{{#getUserAttributes ../members user_id "image"}}{{/getUserAttributes}}" alt="Avatar" class="avatar">

            </div>

        </li>
    </div> --}}