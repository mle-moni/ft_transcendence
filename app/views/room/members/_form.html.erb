<%= form_with(model: room_member, local: true) do |form| %>
  <% if room_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_member.errors.count, "error") %> prohibited this room_member from being saved:</h2>

      <ul>
        <% room_member.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
