<%= form_with(model: room_admin, local: true) do |form| %>
  <% if room_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_admin.errors.count, "error") %> prohibited this room_admin from being saved:</h2>

      <ul>
        <% room_admin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
