<%= form_with(model: room_message, local: true) do |form| %>
  <% if room_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_message.errors.count, "error") %> prohibited this room_message from being saved:</h2>

      <ul>
        <% room_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.text_field :room_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
